---
phase: 10-gap-closure-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PriceBreakdownModal.jsx
  - src/components/MobileBottomBar.jsx
autonomous: true
gap_closure: true
requirements:
  - PRICE-01
  - PRICE-02
  - SUMM-03
  - A11Y-06

must_haves:
  truths:
    - "Modal shows correctly converted prices when client selects non-base currency (e.g., USD amounts convert to EUR)"
    - "Modal final total matches sidebar final total exactly, including when included activities have pax changes"
    - "MobileBottomBar Details and Confirm buttons show visible blue focus ring on keyboard focus"
  artifacts:
    - path: "src/components/PriceBreakdownModal.jsx"
      provides: "Fixed currency conversion and accurate final total"
      contains: "displayPrice(baseQuote)"
    - path: "src/components/MobileBottomBar.jsx"
      provides: "Accessible focus indicators on both buttons"
      contains: "focus-visible:ring-2 focus-visible:ring-blue-500"
  key_links:
    - from: "src/components/PriceBreakdownModal.jsx"
      to: "displayPrice callback from ClientView"
      via: "prop — displayPrice(amount) with no second arg lets ClientView handle conversion from baseCurrency"
      pattern: "displayPrice\\([^,)]+\\)"
    - from: "src/components/PriceBreakdownModal.jsx"
      to: "finalQuote prop from ClientView"
      via: "prop — uses pre-computed finalQuote instead of recalculating"
      pattern: "finalQuote"
---

<objective>
Fix three audit-identified gaps: (1) PriceBreakdownModal currency conversion broken because `selectedCurrency` is passed as second arg to `displayPrice()`, telling it the FROM currency equals the TO currency; (2) Modal's own `finalQuote` calculation excludes included-activity pax change deltas causing mismatch with sidebar; (3) MobileBottomBar buttons lack `focus-visible` focus indicators.

Note: SUMM-03 (Summary tab activity badges) was identified as a gap in the audit but is already implemented — lines 1098-1104 of ClientView.jsx show Included/Optional badges on summary activity cards. No code change needed for SUMM-03.

Purpose: Close all remaining v1.0 milestone audit gaps.
Output: Fixed PriceBreakdownModal.jsx and MobileBottomBar.jsx.
</objective>

<execution_context>
@/Users/veer_vora/.claude/get-shit-done/workflows/execute-plan.md
@/Users/veer_vora/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/PriceBreakdownModal.jsx
@src/components/MobileBottomBar.jsx
@src/pages/ClientView.jsx (lines 410-422 for displayPrice signature, lines 586-597 for finalQuote calculation)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix modal currency conversion and final total divergence</name>
  <files>src/components/PriceBreakdownModal.jsx</files>
  <action>
Two fixes in PriceBreakdownModal.jsx:

**Fix 1 — Currency conversion (PRICE-01):**
Remove `selectedCurrency` as the second argument from ALL `displayPrice()` calls in the component. The `displayPrice` callback from ClientView has signature `(amount, itemCurrency = null)`. When `itemCurrency` is null, it defaults to `baseCurrency` and converts TO `selectedCurrency`. Passing `selectedCurrency` as the second arg makes it think the item's currency IS the selected currency, so no conversion happens.

Find every instance of `displayPrice(someAmount, selectedCurrency)` and change to `displayPrice(someAmount)`. There are ~14 occurrences (lines 144, 164, 165, 176, 189, 220, 221, 231, 238, 268, 296, 309, 324).

Also remove `selectedCurrency` from the component's destructured props since it's no longer needed.

**Fix 2 — Final total match (PRICE-02):**
The modal currently calculates its own `finalQuote` (line 90) using only `optionalActivitiesTotal`, missing included-activity pax change deltas. The sidebar uses `calculateActivityDelta()` which handles included activities with pax changes (returns `currentPrice - basePrice`).

Instead of duplicating that logic, accept `finalQuote` as a new prop from ClientView (it's already computed there as `baseQuote + totalChangeValue`). Use the prop value directly for the FINAL PRICE display instead of `calculations.finalQuote`.

Keep the modal's internal `calculations` for category subtotals (propertyTotal, optionalActivitiesTotal, transportFlightTotal) — those are still useful for line items. Only replace the final total display.

Update the component signature to accept `finalQuote` prop. In ClientView.jsx where PriceBreakdownModal is rendered (line 1584), add `finalQuote={finalQuote}` prop.
  </action>
  <verify>
Run `npx react-scripts build 2>&1 | tail -20` — build succeeds with no errors. Grep for `displayPrice.*selectedCurrency` in PriceBreakdownModal.jsx — zero results. Grep for `finalQuote` prop in ClientView.jsx PriceBreakdownModal usage — present.
  </verify>
  <done>
All displayPrice calls in modal use single-arg form (no selectedCurrency). Modal FINAL PRICE uses prop finalQuote from ClientView. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add focus-visible indicators to MobileBottomBar buttons</name>
  <files>src/components/MobileBottomBar.jsx</files>
  <action>
Update both buttons in MobileBottomBar.jsx:

**Details button (line 24):** Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2` to the existing className.

**Confirm button (line 33):** Replace `focus:outline-none focus:ring-2 focus:ring-blue-500` with `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2`. The change is `focus:` → `focus-visible:` plus adding `ring-offset-2`.

This matches the pattern established in Phase 7 (07-accessibility-fixes) for light-background buttons.
  </action>
  <verify>
Grep MobileBottomBar.jsx for `focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2` — both buttons match. Grep for bare `focus:ring` (without `visible`) — zero results.
  </verify>
  <done>
Both MobileBottomBar buttons have focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 classes. No bare focus: ring classes remain.
  </done>
</task>

</tasks>

<verification>
1. `npx react-scripts build` succeeds with no errors
2. `grep -c "displayPrice.*selectedCurrency" src/components/PriceBreakdownModal.jsx` returns 0
3. `grep -c "focus-visible:ring-2" src/components/MobileBottomBar.jsx` returns 2 (one per button)
4. `grep "finalQuote={finalQuote}" src/pages/ClientView.jsx` shows prop passed to PriceBreakdownModal
</verification>

<success_criteria>
- PriceBreakdownModal displayPrice calls use single-arg form, enabling proper currency conversion
- Modal FINAL PRICE uses ClientView's pre-computed finalQuote (includes all activity deltas)
- Both MobileBottomBar buttons have focus-visible ring indicators
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/10-gap-closure-fixes/10-01-SUMMARY.md`
</output>
